import {
  "Allianaab2m/aya",
  "moonbit-community/sqlite3",
}

import {
  "moonbitlang/async",
} for "test"

// `moonbit-community/sqlite3` vendors the SQLite amalgamation and binds it over
// FFI, so it is a native package.

options(
  backend: [ "native" ],
)
