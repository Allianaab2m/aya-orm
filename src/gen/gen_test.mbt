///|
/// The entity source every test in this file generates from.
let sample : String =
  #|#aya.table(name="users", alias="u")
  #|pub struct User {
  #|  #aya.id
  #|  id : Int
  #|  name : String
  #|  age : Int
  #|  #aya.column(name="deleted_at")
  #|  deleted_at : String?
  #|}
  #|

///|
fn lower(src : String) -> Array[@gen.EntitySpec] raise Error {
  let (impls, reports) = @parser.parse_string(src, name="test.mbt")
  assert_eq(reports.length(), 0)
  @gen.collect_entities(impls)
}

///|
test "attribute parsing keeps namespace and arguments" {
  debug_inspect(
    @gen.parse_attr("#aya.table(name=\"users\", alias=\"u\")"),
    content=(
      #|Some({ name: "table", args: { "name": "users", "alias": "u" } })
    ),
  )
  debug_inspect(
    @gen.parse_attr("#aya.id"),
    content=(
      #|Some({ name: "id", args: {} })
    ),
  )
}

///|
test "attributes from other namespaces are ignored" {
  debug_inspect(@gen.parse_attr("#morm.entity"), content="None")
  debug_inspect(@gen.parse_attr("#deprecated"), content="None")
}

///|
test "lowering records declaration order and the primary key" {
  let specs = lower(sample)
  assert_eq(specs.length(), 1)
  let spec = specs[0]
  assert_eq(spec.type_name, "User")
  assert_eq(spec.table, "users")
  assert_eq(spec.tbl, "u")
  debug_inspect(
    spec.fields.map(f => (f.name, f.ty, f.is_primary)),
    content=(
      #|[
      #|  ("id", "Int", true),
      #|  ("name", "String", false),
      #|  ("age", "Int", false),
      #|  ("deleted_at", "String?", false),
      #|]
    ),
  )
}

///|
test "a struct without #aya.table is skipped" {
  let specs = lower(
    (
      #|pub struct Plain {
      #|  id : Int
      #|}
      #|
    ),
  )
  assert_eq(specs.length(), 0)
}

///|
test "generated source" {
  inspect(
    @gen.emit_file(lower(sample)),
    content=(
      #|// Generated by aya-kit. DO NOT EDIT.
      #|// Edit the annotated entity struct and re-run the generator instead.
      #|
      #|///|
      #|/// Column handles for `User`.
      #|pub struct UserCols {
      #|  id : @aya.Column[Int]
      #|  name : @aya.Column[String]
      #|  age : @aya.Column[Int]
      #|  deleted_at : @aya.Column[String?]
      #|}
      #|
      #|///|
      #|pub fn User::cols() -> UserCols {
      #|  {
      #|    id: @aya.Column::new(tbl="u", name="id"),
      #|    name: @aya.Column::new(tbl="u", name="name"),
      #|    age: @aya.Column::new(tbl="u", name="age"),
      #|    deleted_at: @aya.Column::new(tbl="u", name="deleted_at"),
      #|  }
      #|}
      #|
      #|///|
      #|/// Select every column of `User`, in declaration order.
      #|pub fn User::all(cols : UserCols) -> @aya.Selection[User] {
      #|  @aya.Selection::new(
      #|    [
      #|      cols.id.raw(),
      #|      cols.name.raw(),
      #|      cols.age.raw(),
      #|      cols.deleted_at.raw(),
      #|    ],
      #|    row => User::{
      #|      id: @aya.SqlDecode::decode(row[0], "id"),
      #|      name: @aya.SqlDecode::decode(row[1], "name"),
      #|      age: @aya.SqlDecode::decode(row[2], "age"),
      #|      deleted_at: @aya.SqlDecode::decode(row[3], "deleted_at"),
      #|    },
      #|  )
      #|}
      #|
      #|///|
      #|/// Write every column of `User`, in declaration order.
      #|pub fn User::binding() -> @aya.Binding[User] {
      #|  @aya.Binding::new(
      #|    ["id", "name", "age", "deleted_at"],
      #|    self => [
      #|      @aya.SqlEncode::to_sql_value(self.id),
      #|      @aya.SqlEncode::to_sql_value(self.name),
      #|      @aya.SqlEncode::to_sql_value(self.age),
      #|      @aya.SqlEncode::to_sql_value(self.deleted_at),
      #|    ],
      #|  )
      #|}
      #|
      #|///|
      #|pub fn User::table() -> @aya.Table[UserCols, User] {
      #|  let cols = User::cols()
      #|  @aya.Table::new(table_name="users", tbl="u", cols~, all=User::all(cols), write=User::binding())
      #|}
      #|
      #|///|
      #|/// Build a table over a domain type mapped to and from `User`.
      #|pub fn[D] User::table_of(
      #|  to_domain : (User) -> D raise @aya.DecodeError,
      #|  from_domain : (D) -> User,
      #|) -> @aya.Table[UserCols, D] {
      #|  let cols = User::cols()
      #|  @aya.Table::new(
      #|    table_name="users",
      #|    tbl="u",
      #|    cols~,
      #|    all=User::all(cols).map(to_domain),
      #|    write=User::binding().contramap(from_domain),
      #|  )
      #|}
      #|
      #|///|
      #|pub fn User::primary_key_name() -> String? {
      #|  Some("id")
      #|}
      #|
    ),
  )
}

///|
test "`type=` is refused because the formatter cannot round-trip it" {
  let src =
    #|#aya.table(name="accounts", alias="a")
    #|pub struct Account {
    #|  #aya.column(type="INTEGER")
    #|  id : Int
    #|}
    #|
  try {
    let _ = @gen.entities_of(src, name="test.mbt")
    fail("expected ReservedArg")
  } catch {
    e =>
      inspect(
        e,
        content="`Account`: `type=` cannot be used in an attribute, because `moon fmt` rewrites it into something that will not lex; write `sql_type=` instead",
      )
  }
}

///|
test "an index names fields, and says so when one is missing" {
  let src =
    #|#aya.table(name="users", alias="u")
    #|#aya.index(name="idx_users_nope", columns="nope")
    #|pub struct User {
    #|  #aya.id
    #|  id : Int
    #|}
    #|
  try {
    let _ = @gen.entities_of(src, name="test.mbt")
    fail("expected UnknownIndexColumn")
  } catch {
    e =>
      inspect(
        e,
        content="`User`: #aya.index names `nope`, which is not a field",
      )
  }
}

///|
test "an index defaults its name to what it indexes" {
  let src =
    #|#aya.table(name="users", alias="u")
    #|#aya.index(columns="name, age", unique="true")
    #|pub struct User {
    #|  #aya.id
    #|  id : Int
    #|  #aya.column(name="full_name")
    #|  name : String
    #|  age : Int
    #|}
    #|
  debug_inspect(
    @gen.entities_of(src, name="test.mbt")[0].indexes,
    content=(
      #|[
      #|  {
      #|    name: "idx_users_full_name_age",
      #|    columns: ["full_name", "age"],
      #|    unique: true,
      #|  },
      #|]
    ),
  )
}

///|
test "an attribute value may contain a comma" {
  debug_inspect(
    @gen.parse_attr("#aya.column(default=\"coalesce(a, b)\")"),
    content=(
      #|Some({ name: "column", args: { "default": "coalesce(a, b)" } })
    ),
  )
}
